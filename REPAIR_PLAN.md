# chunklog 论文与核心算法修复计划

制定日期：2026-08-09  
依据：`REVIEW.md` 中的审稿意见  
目标：消除论文、形式模型、代码实现和实验结果之间的不一致，形成可复现、可验证、可重新投稿的研究成果。

## 1. 修复原则

本轮整改不以保留现有论文结论为目标，而以保证结论真实、边界清楚、实验可复现为最高优先级。具体遵循以下原则：

1. **先修正确性，再谈性能。** 路径安全、对象完整性和引用一致性优先于算法优化。
2. **先定义成本模型，再给复杂度。** 严格区分函数调用次数、新对象数量、逻辑字节、实际 I/O、CPU 时间和端到端延迟。
3. **论文主张不得强于实现。** 尚未实现或未验证的性质只能作为未来工作，不得写成已证明或已确认的结论。
4. **所有实验必须代码化。** 论文中的每张表和每个数字都必须能由仓库脚本从干净环境重新生成。
5. **保留可审计证据。** 原始实验数据、环境信息、统计方法和生成脚本随论文版本一同归档。

## 2. 目标成果

整改完成后应交付：

- 一个不存在已知路径遍历和静默对象篡改问题的 `chunklog` 实现；
- 一个语义明确的完整快照 API，以及可选的增量 change-set API；
- 一个与真实实现一致的形式模型和复杂度分析；
- 一套覆盖正确性、完整性、故障和安全边界的测试；
- 一套可以一键复现论文全部表格和图的实验程序；
- 至少一个真实体素世界数据集上的评估；
- 一篇缩小新颖性主张、完整披露限制、可独立复核的修订论文。

## 3. 总体路线

整改分为六个阶段。阶段 1—3 是代码和语义基础，阶段 4 重建实验，阶段 5 重写形式模型和论文，阶段 6 执行最终复现审计。除文献调研外，不应在核心实现尚未稳定时提前固定论文数字。

| 阶段 | 内容 | 优先级 | 主要产物 |
| --- | --- | --- | --- |
| 1 | 冻结主张并建立可追踪基线 | P0 | 主张—证据矩阵、失败测试 |
| 2 | 修复安全性和数据完整性 | P0 | 安全分支 API、哈希校验、原子引用更新 |
| 3 | 明确提交语义并修复核心算法 | P0/P1 | snapshot/change-set API、增量 Tree 设计 |
| 4 | 重建测试与实验体系 | P0 | 可执行实验、原始数据、真实负载评估 |
| 5 | 重写形式模型与论文 | P0 | 新定理、证明、相关工作和修订稿 |
| 6 | 独立复现与投稿前审计 | P0 | 复现报告、最终 artifact、投稿检查表 |

## 4. 阶段 1：冻结主张并建立基线

### 4.1 建立主张—证据矩阵

逐条提取摘要、贡献、定理、实验结论和结论章节中的可验证主张，并记录：

- 主张原文；
- 形式化前提；
- 对应实现位置；
- 对应测试或实验；
- 当前状态：已支持、部分支持、不支持、尚未验证；
- 处理方式：保留、弱化、重写、删除。

至少覆盖以下核心主张：

- 确定性结构共享；
- 任意版本可比较；
- O(k) 增量提交；
- O(1) 逻辑 checkout；
- 仓库增长闭式公式；
- GC soundness、completeness 和所谓 crash atomicity；
- fail-loud corruption；
- “首个完整实例化”及其他新颖性声明。

### 4.2 添加能够暴露已知问题的回归测试

在修复代码前先加入失败测试，至少包括：

- `delete_branch` 拒绝 `..`、路径分隔符和绝对路径；
- `resolve`、`checkout` 不读取 refs 根目录外的路径；
- 修改 blob 文件内容后 `read` 或 `load` 必须报哈希不匹配；
- 修改 Tree/Commit 后必须 fail loud；
- staging 单 chunk 更新不会意外删除未暂存世界，或明确验证完整快照语义；
- 基准生成器产生严格 N 个坐标和 N 个唯一 payload；
- GC sweep 中途失败时表现与文档语义一致。

### 4.3 验收标准

- 主张—证据矩阵覆盖论文全部核心结论；
- 所有已知缺陷均有失败测试或明确的不可测试原因；
- 当前基线测试、工具链版本和 Git 提交哈希被记录。

## 5. 阶段 2：安全性、完整性与引用一致性

### 5.1 统一分支名称验证

所有接受分支名的公共入口统一调用同一个验证函数，包括：

- create；
- delete；
- checkout；
- resolve；
- read ref；
- 从 HEAD 恢复当前分支。

除字符串规则外，还必须对规范化后的目标路径执行 containment 检查，确保目标始终位于 `.chunklog/refs/heads/` 内。不得只依赖对 `/` 和 `\\` 的过滤。

### 5.2 对象完整性验证

调整 `ObjectStore` 语义：

- `read(hash)` 返回数据前重新计算 BLAKE3；
- 计算结果与请求地址不一致时返回明确的完整性错误；
- `write` 遇到已有对象时验证其现有内容，而不是直接成功；
- 测试 blob、Tree、Commit 三类对象的篡改行为；
- 记录完整性校验带来的性能成本，并在实验中单独报告。

评估为对象增加域分离前缀，例如：

- `blob\0 || payload`；
- `tree\0 || encoding`；
- `commit\0 || encoding`。

若为了保持 blob 原始存储而不采用前缀，则必须在形式模型中明确同一字节地址可承担多种上下文角色，并修正对象计数前提。

### 5.3 原子引用更新

为 HEAD 和 branch ref 引入统一的原子写入过程：

1. 在同目录创建唯一临时文件；
2. 写入完整内容；
3. 根据支持情况执行数据同步；
4. 原子替换目标；
5. 根据平台能力同步父目录；
6. 清理遗留临时文件。

同时明确支持模型是单进程单写者、进程级单写者还是允许多进程读。若只支持单写者，应使用仓库锁阻止 commit、checkout、branch update 与 GC 并发执行。

### 5.4 重新定义 GC 故障语义

短期方案：

- 删除 crash-atomicity 声明；
- 将性质定义为 marking failure before sweep；
- 明确 sweep 失败可能导致部分删除，但删除对象均来自已计算的 unreachable 集合；
- 对重复执行 GC 的幂等性给出测试。

长期可选方案：

- 生成带校验的删除 manifest；
- 把待删除对象原子移动到隔离目录；
- 在提交点后清理隔离目录；
- 或使用支持批量事务的后端扩展接口。

只有实现并进行故障注入后，论文才能重新使用 transactional 或 crash-atomic 等术语。

### 5.5 验收标准

- 所有路径遍历测试通过；
- blob 和结构对象的静默篡改均能被检测；
- HEAD/ref 不会因普通进程中断留下部分文本；
- GC 文档、实现和故障测试采用同一语义；
- 安全相关代码通过跨平台测试或明确列出平台限制。

## 6. 阶段 3：提交语义与核心算法

### 6.1 拆分完整快照与增量变更接口

保留兼容性良好的完整快照接口，但对其命名和复杂度进行明确说明：

```text
commit_snapshot(world, message)
```

其端到端复杂度应诚实标为 Θ(N)，即使只有 k 个 payload 发生变化。

新增显式 change-set：

```text
commit_changes(upserts, removals, message)
```

其中：

- `upserts` 表示新增或修改的坐标及 payload；
- `removals` 表示显式删除的坐标；
- 变更集合中不得出现冲突坐标；
- 基于 HEAD 的 Tree 生成新版本；
- detached HEAD 与 unborn branch 的语义必须单独规定。

### 6.2 修复 CLI staging 语义

CLI staging 应改成 patch 语义：

- 普通 `<x>,<z>` 文件表示 upsert；
- 使用明确的删除清单或 tombstone 表示 removal；
- commit 后只在引用更新成功时清除 staging；
- 清理失败不得破坏已提交版本，并应给出可恢复提示；
- 提供 `status` 或等价预览，显示新增、修改和删除坐标。

如果短期不实现 patch，则必须将 CLI 明确改成 snapshot import，并要求 staging 包含完整世界，不再使用“Git index-style”表述。

### 6.3 选择增量 Tree 方案

需要在论文重投稿前决定采用以下哪条路线：

#### 路线 A：保留平面 Tree

- 实现简单；
- 完整快照和 change-set 最终仍需重建、序列化 Θ(N) Tree；
- 只能主张新增 blob 数据 O(k)，不能主张端到端 O(k) commit；
- 论文定位偏工程系统与经验评估。

#### 路线 B：实现持久化内容寻址 Tree

- 使用确定性 B-tree、trie 或 HAMT；
- 单点修改只复制从根到叶的路径；
- 需要规定节点大小、分裂规则和规范化编码；
- 提交复杂度预计为 O(k log N) 或与树高相关，而不能未经证明直接写 O(k)；
- diff 可利用相同子树哈希跳过未变化区域；
- GC 需要遍历多级 Tree 节点；
- Theorem 3 必须替换为节点级增长界限。

建议优先采用路线 B，因为它能消除当前论文最核心的理论—实现矛盾。但必须先完成原型和实验，再确定最终复杂度表述。

### 6.4 明确 checkout 的含义

论文必须持续区分：

- logical checkout：只更新 HEAD/reference；
- validation/resolution：读取并验证目标 ref/commit；
- materialization：加载完整世界或按需加载 chunk；
- engine activation：让真实引擎切换到目标世界。

可保留“相对于世界 chunk 数量 N 为 O(1)”的结论，但应注明其仍与引用长度、Commit 大小、文件系统调用及完整性验证有关。不得把逻辑 checkout 的延迟等同于用户可见的完整世界恢复延迟。

### 6.5 验收标准

- snapshot 和 change-set 的语义、错误条件与复杂度分别记录；
- CLI 不会因只暂存一个 chunk 而意外删除整个世界；
- 若采用持久化 Tree，随机变更后的 materialization 与参考 `HashMap` 始终一致；
- no-op、revert、删除、重新添加和分支提交均有覆盖；
- 实现复杂度与论文准备采用的定理一致。

## 7. 阶段 4：测试与实验体系重建

### 7.1 修复数据生成器

新的合成数据生成器必须满足：

- 精确生成 N 个坐标，不使用 `ceil(sqrt(N))²` 替代；
- payload 由坐标、版本号和随机种子共同决定；
- 可以控制唯一率、重复率、编辑密度和压缩后大小；
- 固定种子时结果完全可复现；
- 测试主动验证唯一 payload 数量，而不是依赖注释。

### 7.2 建立实验矩阵

至少覆盖：

| 变量 | 建议取值 |
| --- | --- |
| 世界规模 N | 10²、10³、10⁴、10⁵，条件允许时增加 10⁶ |
| 修改数量 k | 0、1、10、1%、10%、100% |
| payload 大小 | 256 B、4 KiB、16 KiB、64 KiB |
| payload 唯一率 | 10%、50%、100% |
| 历史长度 R | 1、10、50、100、1,000 |
| 存储后端 | FilesystemStore、MemoryStore，必要时增加模拟延迟后端 |
| 工作负载 | synthetic、真实引擎世界、回退后再编辑 |

### 7.3 重新设计 benchmark

分别测量：

- initial full commit；
- unchanged snapshot commit；
- k-change snapshot commit；
- k-change change-set commit；
- load full world；
- lazy chunk lookup 和单 chunk load；
- branch-to-branch logical checkout；
- commit-hash detached checkout；
- diff：相同版本、稀疏变化、完全不同；
- GC：全可达、少量垃圾、大量垃圾；
- integrity verification overhead。

基准 setup 不进入计时区；各方案必须使用一致的目录生命周期、缓存策略、持久性等级和数据规模。naive baseline 需要明确是覆盖当前快照、创建新快照目录，还是复制完整历史，不能混合不同语义。

### 7.4 存储增长实验

存储实验同时报告：

- 逻辑对象数量；
- 唯一 blob 数量；
- Tree 节点数量；
- Commit 数量；
- 各对象类型逻辑字节；
- 文件系统实际分配字节；
- 临时文件和索引开销；
- 相对于 full-copy、Git 或其他基线的比率。

若保留平面 Tree，明确展示 Θ(RN) 元数据项。若使用持久化 Tree，则测量节点分裂和结构共享产生的实际增长，并与理论上界比较。

### 7.5 真实工作负载

至少接入一个可公开或可生成的真实体素世界数据源。记录：

- 引擎和世界格式版本；
- chunk 提取及序列化方法；
- 世界总 chunk 数；
- payload 大小分布；
- 重复率；
- 每次保存的实际编辑密度；
- materialization 和恢复过程。

真实数据实验用于验证外部有效性，不能只用 headless toy example 替代。

### 7.6 统计和 artifact 要求

- 固定 Criterion 版本；
- 报告 warm-up、measurement time 和 sample size；
- 报告中位数及置信区间，不只列单点；
- 记录 CPU、内存、操作系统、文件系统和存储介质；
- 记录 Rust 版本、依赖锁文件和 Git commit；
- 保存原始 JSON/CSV；
- 提供生成 Markdown/LaTeX 表格和图形的脚本；
- 在 CI 中运行小规模 correctness experiment；
- 提供单命令复现入口，例如 `cargo xtask paper`。

### 7.7 验收标准

- 从干净 clone 可以生成论文全部表格；
- 合成数据生成器通过规模和唯一性断言；
- 论文数字与生成 artifact 完全一致；
- 至少一名未参与实现的人完成复现；
- 性能结论同时包含统计不确定性和适用边界。

## 8. 阶段 5：形式模型与论文重写

### 8.1 重建成本模型

论文应为每个操作分别定义：

- N：当前版本的坐标数量；
- k：新增、修改和删除坐标的总数；
- s：payload 字节大小或其分布；
- R：历史保存次数；
- h：持久化 Tree 高度；
- B：Tree 节点容量；
- K：全局唯一的新 payload 数；
- 存储操作次数；
- 读取和写入字节数；
- 哈希字节数；
- 内存工作集。

所有大 O 结论必须说明针对哪一种资源，不再使用笼统的“commit cost”。

### 8.2 重写定理

根据最终实现选择定理版本。

若保留平面 Tree：

- 定理 1：新增 blob 数或新增 blob 字节界限；
- 推论：snapshot commit 端到端 Θ(N)；
- Tree 历史字节 Θ(RN)；
- 删除 O(k) 提交主张。

若采用持久化 Tree：

- 证明规范编码和根哈希确定性；
- 证明单次 change-set 修改的节点复制界限；
- 给出 O(k log N) 或更精确的节点/字节复杂度；
- 证明 diff 对共享子树的跳过性质；
- 重新证明包含多级节点的 GC soundness/completeness；
- 重新推导历史增长上界。

### 8.3 修正 GC 定理

将以下性质分开陈述：

1. 静态根集合、无并发修改条件下的 soundness；
2. `list()` 完整且 delete 全部成功条件下的 completeness；
3. marking failure before sweep；
4. sweep 中断后的安全性和可重试性；
5. 若实现事务化方案，再单独证明其提交点与恢复性质。

必须明确单写者假设、根集合快照时刻以及 GC 与 commit/ref update 的互斥关系。

### 8.4 修正对象模型

形式模型应明确：

- 对象是否具有类型域分离；
- 地址是对原始 payload 还是带标签编码求哈希；
- Store 读取是否验证地址与内容一致；
- 不可信或损坏存储的错误语义；
- Commit message 和 timestamp 是否有大小及规范化约束；
- 序列化格式升级如何影响地址稳定性；
- 不同版本的 postcard/Serde 是否保证跨版本规范编码。

如果论文继续声称跨进程、跨机器、跨版本可重复，应固定并版本化 canonical wire format，而不能仅依赖某个序列化库当前实现。

### 8.5 收缩新颖性主张

建议将贡献重新定位为：

1. 对键/坐标寻址世界状态的明确版本化接口和语义；
2. 一个引擎无关的 Rust 实现；
3. 对平面 Tree 与持久化 Tree、chunk 粒度和元数据成本的实证分析；
4. 在真实体素工作负载上的可复现实验；
5. 对逻辑 checkout 与物理 materialization 的系统区分。

不再笼统声称首次提出 CAS、Merkle 版本图、任意快照 diff 或 mark-and-sweep。若保留“首个”声明，必须定义精确的功能组合、检索范围和截止日期，并列出最接近的系统。

### 8.6 重写实验章节

实验章节围绕研究问题组织，而不是围绕已有表格组织：

- RQ1：实现是否满足版本、共享、完整性和 GC 不变量？
- RQ2：提交成本如何随 N、k、s 和唯一率变化？
- RQ3：Tree 表示如何影响时间和历史存储增长？
- RQ4：logical checkout 与 materialization 的实际成本分别是多少？
- RQ5：真实体素工作负载相对 full-copy/Git 基线有何收益和代价？
- RQ6：完整性校验和原子性措施带来多少开销？

每个研究问题都应给出实验设计、控制变量、统计结果、解释和威胁，而不是用有限数据“确认定理”。实验可以支持与理论一致，但不能证明渐近复杂度定理。

### 8.7 验收标准

- 摘要、贡献和结论不包含超出证据的主张；
- 所有定理与最终代码的数据结构和 API 对应；
- 每个实验数字都有 artifact 来源；
- 限制章节包含并发、崩溃、格式演进、真实集成和小 payload 元数据开销；
- 相关工作覆盖学术系统和实际世界版本工具。

## 9. 阶段 6：投稿前独立审计

### 9.1 工程审计

执行并保存结果：

```text
cargo test --all-targets
cargo test --doc
cargo clippy --all-targets -- -D warnings
cargo fmt --all -- --check
cargo doc --no-deps
```

此外执行：

- property-based 状态机测试；
- 路径和对象内容 fuzzing；
- 故障注入；
- 大历史和深分支压力测试；
- Windows 与至少一个 Unix 平台测试；
- 从旧格式升级和未知格式拒绝测试。

### 9.2 论文—代码一致性审计

由独立复核者逐项检查：

- 论文伪代码是否对应真实代码；
- 数据结构是否一致；
- 所有复杂度变量是否定义；
- 所有实验版本是否固定；
- 表格数字是否来自归档数据；
- README 是否与论文采用相同数字和术语；
- 不存在“测试通过”被错误表述为“定理得到证明”的情况。

### 9.3 最终决策门槛

只有同时满足以下条件才进入重投稿：

- P0 缺陷全部关闭；
- 无已知静默数据损坏路径；
- 复杂度主张与实现一致；
- 论文全部实验可从干净环境复现；
- 真实工作负载实验完成；
- 独立复核没有发现重大论文—代码偏差；
- 摘要和结论已经根据最终数据重新撰写。

## 10. 建议的实施顺序

建议按以下顺序推进，避免在不稳定实现上重复跑实验：

1. 建立主张—证据矩阵和失败测试；
2. 修复路径遍历和对象完整性；
3. 实现原子 ref 写入与仓库锁；
4. 明确并修复 staging 语义；
5. 决定平面 Tree 或持久化 Tree 路线；
6. 完成提交 API 和 Tree 实现；
7. 扩展正确性、属性和故障测试；
8. 修复并版本化实验生成器；
9. 完成 synthetic 与真实负载实验；
10. 重写形式模型、定理和证明；
11. 重写实验、相关工作、摘要和结论；
12. 执行独立复现和投稿前审计。

## 11. 第一轮里程碑

第一轮不追求完成论文重写，而是建立一个可信的工程基线。建议第一轮完成以下内容：

- 所有分支入口统一验证并通过路径安全测试；
- `FilesystemStore` 能检测 blob、Tree、Commit 篡改；
- HEAD 和 branch ref 使用统一原子更新函数；
- CLI staging 语义确定并有回归测试；
- benchmark 数据生成器能严格生成 N 个唯一 payload；
- 论文中 O(k)、crash-atomicity 和 Criterion 版本等不实主张被标记为待重写；
- 建立可持续更新的主张—证据矩阵。

第一轮完成后，再根据持久化 Tree 原型的复杂度和实际表现决定论文采用路线 A 还是路线 B。
