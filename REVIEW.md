# 《Content-Addressed Versioning for Structured World State》审稿意见

审稿日期：2026-08-09  
审稿对象：`paper.md` 及当前 `chunklog` 实现  
审稿建议：**拒稿后重投（Reject and Resubmit / Major Reconstruction）**  
审稿信心：**4.5/5**

## 1. 总体评价

本文提出一种面向体素世界及一般键值化结构状态的内容寻址版本模型，并以 Rust 库和 CLI `chunklog` 作为实现。其基本思路是把世界表示为坐标到不透明字节载荷的映射，以 Blob、Tree、Commit 形成不可变对象图，通过可变引用实现版本切换，并使用 mark-and-sweep 回收不可达对象。

项目的代码结构清晰，`ObjectStore` 抽象简洁，Tree 使用 `BTreeMap` 保证确定性序列化顺序，引用切换与物化分离也具有合理的工程价值。当前 32 个集成测试和 1 个 doctest 均通过，Clippy（warnings as errors）和格式检查也通过。

但是，论文的核心复杂度结论、崩溃原子性主张和实验结果目前不足以成立。问题并非局部措辞或少量补充实验，而是理论模型、公开接口、代码实现与基准方法之间存在系统性偏差。若定位为开源工程或短篇系统演示，该项目具有进一步发展的价值；若作为完整研究论文投稿，当前版本尚未达到可接收标准。

## 2. 主要问题

### 2.1 “O(k) 增量提交”结论不成立

论文 Theorem 1 将增量提交概括为 O(k)，并声称 blob I/O 与修改 chunk 数量成正比。然而实际的 `commit(&World)` 接口接收完整世界，并执行以下工作：

1. 遍历全部 N 个 chunk；
2. 对全部 payload 重新计算哈希并调用 `store.write`；
3. 构造包含 N 个条目的完整 Tree；
4. 序列化、哈希并写入 Θ(N) 大小的 Tree；
5. 创建 Commit 并更新引用。

因此当前实现至少包含 N 次 payload 哈希、N 次 `write`/存在性检查以及 Θ(N) 的 Tree 字节写入。只有“新创建的 blob 文件数量”可以是 O(k)，但总体 CPU、元数据访问和字节 I/O 仍为 Θ(N)。特别是，Tree publication 本身就需要写入 Θ(N) 字节，不能被归入 O(k) 的存储复杂度。

建议将结论严格改写为：

> 新增 blob 数据量为 O(k)，但当前完整快照 API 的提交时间、payload 扫描和 Tree 元数据 I/O 均为 Θ(N)。

若要实现真正的 O(k) 增量提交，需要让接口接受 change set，并将 Tree 改造成持久化 Merkle Tree、B-tree、HAMT 或其他支持路径复制的增量索引。

### 2.2 GC 的“崩溃原子性”不成立

论文 Theorem 5 一方面声称删除“全部发生或全部不发生”，另一方面又承认 sweep 阶段失败可能留下已经删除的前缀。这两个陈述相互矛盾。

实现中的 GC 只是遍历对象并逐个调用 `delete`，没有事务日志、删除清单、隔离目录、提交点或回滚机制。因此可以成立的性质仅为：

> 标记阶段 fail-closed；如果对象读取或解析失败，算法在开始 sweep 前退出。

这不等同于 crash atomicity。对象写入采用 temp file + rename，也不能证明 GC 删除原子性；当前实现没有 `fsync`，因而连掉电后的持久性也没有得到严格保证。

建议删除“crash-atomic”主张，或实现事务化 GC 协议并通过系统性故障注入验证。

### 2.3 基准数据生成器存在致命错误

`benches/storage.rs` 声称生成彼此不同的 256 字节 chunk payload，但内部 `u8` 计数器恰好每 256 次回绕一次，而每个 payload 的长度也恰好为 256。因此每个 chunk 实际包含完全相同的字节序列。

这会产生以下后果：

- 100、1,000、10,000 chunk 的世界实际上只有一个唯一 blob；
- 基准测量的是极端重复数据，而不是论文声称的 distinct chunks；
- CAS commit 与“每 chunk 一个文件”的 naive baseline 之间缺乏公平性；
- 输入规模 1,000 会因为 `ceil(sqrt(size))²` 实际生成 1,024 个 chunk；
- checkout benchmark 只是 checkout 已经处于的 `main`，而不是在两个版本之间切换。

此外，论文 Table 2、Table 3 没有对应的可执行实验或表格生成脚本；论文称使用 Criterion 0.5，而当前 `Cargo.toml` 使用 Criterion 0.8；README、论文表格和本地 Criterion artifact 还存在不同的结果数字。论文也没有充分报告 CPU、内存、存储介质、样本数、置信区间、代码提交哈希和原始数据。

因此，当前所有性能数字以及“精确匹配闭式公式”的实证结论都应视为尚未得到可信验证。

### 2.4 对象数量公式掩盖了 Θ(RN) 的 Tree 字节开销

每次保存都会存储一个完整的 N-entry Tree，因此累计 Tree 元数据为 Θ(RN) 字节。论文给出的 `N + Rk + 2(R+1)` 仅计算对象数量，把一个包含 N 个条目的 Tree 计为“一个对象”，容易造成存储扩展性优于实际情况的印象。

论文自身的字节表达式包含 `R·|tree|`，但随后又称元数据增长“linear in saves, not world size × saves”。由于 `|tree| = Θ(N)`，二者直接矛盾。

论文应分别报告：

- blob 对象数量；
- Tree/Commit 数量；
- blob 逻辑字节；
- Tree/Commit 逻辑字节；
- 文件系统实际分配空间；
- 总仓库字节复杂度 Θ((N + Rk)s + RN)。

### 2.5 对未来编码方案的理论兼容性判断错误

论文称 CDC、跨版本 delta 和持久化 Tree 只改变 blob 内容、不改变对象图，因此 Theorems 1–5 保持不变。该判断不正确：

- CDC 会引入 payload 到 block 的引用；
- 跨版本 delta 会引入 delta 到 base 的依赖；
- 持久化 Tree 会引入多级 Tree 节点及节点间引用；
- GC 必须遍历新增的边；
- materialization 的过程、复杂度和失败条件都会改变；
- 对象增长的闭式公式也随之改变。

这些扩展可以保留论文的高层思想，但不能在不修改形式模型、对象类型和证明的情况下直接继承当前 Theorems 3–5。

### 2.6 分支操作存在严重路径遍历漏洞

当前只有 `create_branch` 调用了 `validate_branch_name`。`delete_branch` 会把未经验证的用户输入直接拼接到 refs 路径并调用 `remove_file`。因此包含 `..` 的相对路径或绝对路径可能使 CLI 删除 `.chunklog/refs/heads` 之外的文件。

`resolve` 和 `checkout` 也会先调用未经验证的 `read_ref`，可能在分支名验证前读取 refs 目录之外的路径。

这与论文所称“任何文件系统访问前均排除路径遍历”直接矛盾，也是发布前必须修复的 P0 安全问题。所有公共分支操作应统一进行名称验证，并在规范化后确认目标仍处于 refs 根目录之内。

### 2.7 内容寻址对象缺少读取完整性验证

`FilesystemStore::read` 只读取文件内容，不重新验证其 BLAKE3 是否等于文件名；`write` 遇到已经存在的目标文件时也直接信任现有内容。

这意味着：

- 被篡改但仍可反序列化的 Tree/Commit 可能被接受；
- blob 被篡改后，`load` 会静默返回错误世界数据；
- 论文中的 content-addressed integrity 和 fail-loud corruption 语义并未实现。

建议在读取时重新计算并验证哈希，在复用已有对象时验证其内容，同时为 Blob、Tree、Commit 引入显式的类型域分离，以避免不同逻辑对象种类共享同一原始字节命名空间所造成的模型歧义。

### 2.8 CLI staging 语义与 Git index 类比不符

CLI 将 staging 目录中的文件直接作为完整 World 提交。假设当前版本有 1,000 个 chunk，而 staging 中只有一个修改后的 chunk，则新版本将只剩这一个 chunk，其余 999 个都会被解释为删除。

这不是 Git index 风格的增量 staging。实现应选择以下一种明确语义：

1. 读取 HEAD Tree，将 staging 作为 patch 合并，并提供显式删除标记；或
2. 明确把 staging 称为“完整快照输入目录”，删除 Git index 类比和增量保存暗示。

## 3. 形式模型评价

论文的形式化具有一定组织作用，但多数结论是 snapshot + content addressing 定义的直接推论：

- 确定性结构共享来自确定性序列化和哈希；
- 任意两个版本可比较来自版本是完整快照映射；
- O(1) 逻辑切换来自引用重写；
- GC soundness 来自标准 reachability mark-and-sweep。

这些性质本身是正确且有用的，但理论新颖性较弱。更重要的是，论文常把“对象操作次数”“新增文件数量”“逻辑数据量”“实际字节 I/O”和“端到端运行时间”混为一谈。正式版本必须先定义成本模型，再分别证明各指标的界限。

Theorem 3 的精确对象数量还需要补充更严格的前提，例如：

- 每次编辑后的新 payload 在全局范围内互不相同；
- Tree 不会与历史 Tree 重合；
- Commit 编码保持唯一；
- 不同对象类型采用域分离，或者允许跨类型字节相等时修正计数；
- 忽略临时文件、损坏对象和文件系统元数据。

“chunk-level dedup 是 optimal”的表述也缺乏明确的最优性目标、对照算法及下界证明，应改为受条件约束的工程适用性结论。

## 4. 新颖性与相关工作

核心设计本质上是“以坐标为键的 Git/Merkle 快照”。论文需要更谨慎地区分：

- 新的基础理论；
- 已知 CAS/Merkle-DAG 性质在体素场景的实例化；
- 新的工程 API 或存储布局；
- 与现有游戏世界版本控制工具相比的实际差异。

当前“首个实现”的检索和论证不充分。现有生态已经存在把 Minecraft 世界纳入 Git、提供快照/分支/恢复的工具，以及将 Bedrock 世界拆成 chunk 以便版本控制的工具。这些项目不必然否定 `chunklog` 的通用存储接口价值，但要求论文缩小 novelty claim，并进行系统性的功能、存储粒度、物化方式和性能对比。

## 5. 实验与可复现性建议

建议重建实验体系，并提供一条命令生成论文全部表格和图：

1. 修复 payload 生成器，以 chunk 坐标或唯一序号作为种子；
2. 确保生成的世界大小严格等于请求的 N；
3. 分别测量 full commit、no-op commit 和 k-change incremental commit；
4. 分别统计哈希时间、对象存在性检查、blob 写入、Tree 序列化和 ref 更新；
5. checkout 应在两个真实分支或版本之间交替；
6. naive baseline 应使用同等持久性、目录生命周期和缓存条件；
7. 增加 Git、压缩全量快照和可用 COW 方案作为基线；
8. 报告中位数、置信区间、样本数、预热策略和异常值策略；
9. 保存 Criterion 原始数据及表格生成脚本；
10. 使用至少一个真实 Minetest、Minecraft 或 Veloren 世界数据集；
11. 测量不同 payload 大小、重复率、编辑密度和世界规模；
12. 增加 crash/fault injection，而不仅是正常路径单元测试。

## 6. 值得肯定的部分

- Rust 模块划分清晰，整体代码易于审计；
- `ObjectStore` 是简洁、可替换的存储抽象；
- `BTreeMap` 能提供稳定的坐标排序和确定性编码；
- 引用切换与世界物化分离是合理的接口设计；
- GC 能保留所有分支可达对象，而不仅是当前 HEAD；
- 当前 32 个集成测试与 1 个 doctest 全部通过；
- `cargo clippy --all-targets -- -D warnings` 通过；
- `cargo fmt --all -- --check` 通过；
- 禁用 unsafe，有利于缩小实现风险面。

这些优点使其具备继续发展为可靠研究原型的基础，但尚不能补偿核心结论和实验方法中的问题。

## 7. 修改优先级

### P0：提交或发布前必须完成

1. 修复所有分支 API 的路径遍历问题；
2. 加入对象读取及已有对象复用时的哈希验证；
3. 撤回或严格重写 O(k) 与 crash-atomicity 主张；
4. 修复基准生成器并重新运行全部实验；
5. 明确 CLI staging 是 patch 还是完整快照；
6. 纠正 Θ(RN) Tree 元数据复杂度和相关文字；
7. 为论文中的每张实验表提供可执行生成程序。

### P1：建议作为重投稿的核心改进

1. 实现真正的 change-set API 和增量持久化 Tree；
2. 引入原子 ref 更新、唯一临时文件、仓库锁和持久化同步；
3. 增加属性测试、恶意路径测试、对象篡改测试和故障注入；
4. 引入真实引擎数据及公平基线；
5. 重新定义 CDC/delta/持久化 Tree 扩展后的对象图与 GC；
6. 缩小新颖性主张，并完善实践系统相关工作。

## 8. 评分

| 维度 | 评分 |
| --- | ---: |
| 技术正确性 | 1.5/5 |
| 实验可信度 | 1/5 |
| 新颖性 | 2/5 |
| 工程质量 | 2.5/5 |
| 表达清晰度 | 3/5 |
| 可复现性 | 1.5/5 |

## 9. 最终决定

**当前版本建议拒稿。**

本文具备清晰的工程动机和可继续发展的实现基础，但必须重新界定复杂度、修正安全与完整性问题、重建实验，并使形式模型与实际算法保持一致。在完成上述重构后，建议以“面向结构化世界状态的工程化 CAS 版本系统及真实工作负载评估”为更准确的定位重新投稿。
